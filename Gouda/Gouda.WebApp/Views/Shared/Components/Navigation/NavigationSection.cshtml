@model NavigationSection
<gouda-nav-section>
    <div class="title">@Model.Name</div>
    <ul class="node-list">
        @foreach (NavigationNode node in Model.Nodes)
        {
            <li>@await Component.InvokeAsync(NavigationViewComponent.Name, node)</li>
        }
    </ul>
</gouda-nav-section>