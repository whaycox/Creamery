namespace Curds.Persistence.Query.Domain
{
    public enum SqlQueryKeyword
    {
        CREATE,
        DROP,
        TABLE,
        INSERT,
        OUTPUT,
        INTO,
        VALUES,
        SELECT,
        DELETE,
        UPDATE,
        SET,
        FROM,
        JOIN,
        ON,
        WHERE,

        AND,
        OR,

        inserted,
    }
}
